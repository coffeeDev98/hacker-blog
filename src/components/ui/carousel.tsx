import { Hit } from "@/types/blog";
import React from "react";

type Props = { data: Hit[] };

const Carousel = (props: Props) => {
  return <div className="border-y border-gunmetal"></div>;
};

export default Carousel;
